import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); async function run() { const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const { data } = await supabase.from('Staff').select('id, name, type, ktv_shift_1_bonus, ktv_shift_2_bonus, ktv_shift_3_bonus').eq('id', 'NH021').single(); console.log(JSON.stringify(data, null, 2)); } run();
