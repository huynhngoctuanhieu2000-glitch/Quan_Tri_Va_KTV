import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL || '', process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || ''); async function main() { const today = new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Ho_Chi_Minh' }); const { data: bookings } = await supabase.from('Bookings').select('id, billCode, status').gte('bookingDate', today + ' 00:00:00'); console.log(bookings); } main().then(() => process.exit(0)).catch(console.error);
