import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from('Bookings').select('*, BookingItems!fk_bookingitems_booking(*)').eq('id', '11NDK-003-20082026-A').single(); const bConfig = { s1Bonus: 20, s2Bonus: 20, s3Bonus: 30, enableBonus: true }; data.rating = 5; data.BookingItems.forEach(i => i.itemRating = 5); const { KtvCommissionService } = await import('./lib/services/KtvCommissionService.ts'); console.log('Bonus NH027:', KtvCommissionService.calculateBookingBonus(data, 'NH027', '2026-08-20', [], bConfig, {}, {}, true)); } run();
