import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); async function run() { const { KtvCommissionService } = await import('./lib/services/KtvCommissionService.ts'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const { data: b } = await supabase.from('Bookings').select('*').eq('id', '11NDK-003-20082026-A').single(); const { data: allItems } = await supabase.from('BookingItems').select('*').eq('bookingId', b.id); const fullBooking = { ...b, BookingItems: allItems }; const bConfig = { s1Bonus: 20, s2Bonus: 20, s3Bonus: 30, enableBonus: true }; console.log('Bonus NH021 without targetGuestId:', KtvCommissionService.calculateBookingBonus(fullBooking, 'NH021', '2026-08-20', [], bConfig, {}, {}, true)); } run();
