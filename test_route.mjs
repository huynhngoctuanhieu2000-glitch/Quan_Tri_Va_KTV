import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data: b } = await supabase.from('Bookings').select('*').eq('id', 'BK-11NDK-001-20082026').single(); const { data: allItems } = await supabase.from('BookingItems').select('*').eq('bookingId', b.id); const fullBooking = { ...b, BookingItems: allItems }; fullBooking.rating = 5; allItems.forEach(i => i.itemRating = 5); const bConfig = { s1Bonus: 10, s2Bonus: 20, s3Bonus: 30, enableBonus: true }; const { KtvCommissionService } = await import('./lib/services/KtvCommissionService.ts'); console.log('Bonus NH021:', KtvCommissionService.calculateBookingBonus(fullBooking, 'NH021', '2026-08-20', [], bConfig, {}, {}, true)); } run();
